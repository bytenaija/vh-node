var express = require('express');
var router = express.Router();
let EventsController = require('../controllers')

// Route related to delete events
router.delete('/erase', )

module.exports = router;